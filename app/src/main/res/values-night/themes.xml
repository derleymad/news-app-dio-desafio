<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NewsApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/back_ground_dark</item>

        <item name="colorPrimary">@color/primaryColorDarkMode</item>
        <item name="colorPrimaryVariant">@color/primaryDarkColorDarkMode</item>
        <item name="colorOnPrimary">@color/primaryTextColorDarkMode</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorSecondaryVariant">@color/secondaryDarkColor</item>
        <item name="colorOnSecondary">@color/secondaryTextColor</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/black</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="splashScreen" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_image_dark</item>
        <item name="android:statusBarColor">@color/back_ground_dark</item>
    </style>
</resources>